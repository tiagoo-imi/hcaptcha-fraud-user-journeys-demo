/**
 * API base URL.
 * - Leave empty ("") to use same-origin (recommended when deploying to Azure Static Web Apps with integrated Functions).
 * - Or set to full URL (e.g., "https://your-api.example.com")
 */
export const API_BASE_URL = "";
